diff --git a/mvadapter/utils/mesh_utils/__init__.py b/mvadapter/utils/mesh_utils/__init__.py
index dd4bbde..ba21d14 100644
--- a/mvadapter/utils/mesh_utils/__init__.py
+++ b/mvadapter/utils/mesh_utils/__init__.py
@@ -1,3 +1,5 @@
+from importlib import import_module
+
 from .camera import (
     Camera,
     get_c2w,
@@ -6,15 +8,39 @@ from .camera import (
     get_orthogonal_projection_matrix,
     get_projection_matrix,
 )
-from .mesh import TexturedMesh, load_mesh, replace_mesh_texture_and_save
-from .projection import CameraProjection, CameraProjectionOutput
-from .render import (
-    DepthControlNetNormalization,
-    DepthNormalizationStrategy,
-    NVDiffRastContextWrapper,
-    RenderOutput,
-    SimpleNormalization,
-    Zero123PlusPlusNormalization,
-    render,
-)
-from .smart_paint import SmartPainter
+
+_LAZY_EXPORTS = {
+    "TexturedMesh": (".mesh", "TexturedMesh"),
+    "load_mesh": (".mesh", "load_mesh"),
+    "replace_mesh_texture_and_save": (".mesh", "replace_mesh_texture_and_save"),
+    "CameraProjection": (".projection", "CameraProjection"),
+    "CameraProjectionOutput": (".projection", "CameraProjectionOutput"),
+    "DepthControlNetNormalization": (".render", "DepthControlNetNormalization"),
+    "DepthNormalizationStrategy": (".render", "DepthNormalizationStrategy"),
+    "NVDiffRastContextWrapper": (".render", "NVDiffRastContextWrapper"),
+    "RenderOutput": (".render", "RenderOutput"),
+    "SimpleNormalization": (".render", "SimpleNormalization"),
+    "Zero123PlusPlusNormalization": (".render", "Zero123PlusPlusNormalization"),
+    "render": (".render", "render"),
+    "SmartPainter": (".smart_paint", "SmartPainter"),
+}
+
+
+def __getattr__(name):
+    if name not in _LAZY_EXPORTS:
+        raise AttributeError(f"module {__name__!r} has no attribute {name!r}")
+
+    module_name, attribute_name = _LAZY_EXPORTS[name]
+    try:
+        module = import_module(module_name, __name__)
+    except ModuleNotFoundError as exc:
+        if exc.name and exc.name.startswith("nvdiffrast"):
+            raise RuntimeError(
+                "nvdiffrast is required for MV-Adapter geometry/rasterization paths; "
+                "camera-only i2mv inference does not require it."
+            ) from exc
+        raise
+
+    value = getattr(module, attribute_name)
+    globals()[name] = value
+    return value
diff --git a/mvadapter/utils/mesh_utils/utils.py b/mvadapter/utils/mesh_utils/utils.py
index fcb965f..26737d3 100644
--- a/mvadapter/utils/mesh_utils/utils.py
+++ b/mvadapter/utils/mesh_utils/utils.py
@@ -7,13 +7,20 @@ from datetime import datetime
 from typing import List, Optional, Union
 
 import numpy as np
-import nvdiffrast.torch as dr
 import torch
 import torch.nn.functional as F
 import trimesh
 from PIL import Image
 from torch import BoolTensor, FloatTensor
 
+try:
+    import nvdiffrast.torch as dr
+except ModuleNotFoundError as exc:
+    if exc.name and exc.name.startswith("nvdiffrast"):
+        dr = None
+    else:
+        raise
+
 LIST_TYPE = Union[list, np.ndarray, torch.Tensor]
 IMAGE_TYPE = Union[Image.Image, List[Image.Image], np.ndarray, torch.Tensor]
 SINGLE_IMAGE_TYPE = Union[Image.Image, np.ndarray, torch.Tensor]
