<!--
input: 论文解读 HTML 的新建、重写或排版修复
output: 轻量版式约束
pos: paper-reading 的页面版式标准
-->

# 论文 HTML 版式标准

## 目标

- 页面首先是一篇可连续阅读的技术长文。
- 正文是主层；批注、划线和作图补充是辅助层。
- SVG 是正文配图，不做装饰。

## 首屏

- 先出现一句话结论，再说明研究问题、作者核心动作和实验可信度。
- 提供明确阅读建议：推荐精读、选择性精读、背景参考或暂不投入。
- 标题克制，副标题说明阅读对象、任务边界和材料来源。
- 不做营销 hero，不做装饰性仪表盘，不让版式取代内容判断。

## 阅读地图

- 左侧是阅读决策地图，不是论文原始章节目录。
- 优先覆盖：结论、论文地图、Abstract、Experiment、作者论证、方法、技术位置、综合评价。
- 未进入方法精读时，不显示空的 Approach 或公式章节。
- 空间不足时收起为左边缘浮窗。

## 版面

- `<1120px`：单栏阅读，批注回到正文下方。
- `1180px-1499px`：左侧地图 + 正文 + 右侧批注；避免正文被批注挤压。
- `>=1500px`：增加留白，不拉长正文。

## 批注

- 同一条疑问和回答放进同一个 `paper-annotation`。
- `paper-question-marker` 要小而明显；不显示原文、章节和时间戳。
- 正文高亮用半高划线式背景，不做整行实心色块。
- 批注回答只补当前标记，不复述整段正文；如果解释变长，应拆入正文小节。

## 概念段落

- 概念底座优先使用短小标题，把定义、约束、例子和结论分开。
- 同一段不要同时解释三个以上关键术语。
- 一个正文段落只承担一个任务：定义、机制、例子、边界或结论。
- 标记解释贴近对应概念，不集中堆在一段后面。
- 英文术语用 `term-bridge` 类型的解释块承接：英译中、怎么理解、避免误解。
- 概念图用小型 `svg-figure concept-visual`，只画动作链、因果链或取舍关系，不做装饰图。

## 公式

- 核心公式使用独立的教科书式解释框（`formula-card`），不混在普通段落里。
- 公式本体采用 KaTeX 双层渲染：`equation-rendered`（数学排版）优先显示，`equation-plain`（纯文字）用于回退和无障碍。详见 [katex.md](katex.md)。
- 解释框包含：公式本体（双层）、公式在回答什么问题、变量表、直觉读法。
- `data-katex` 属性必须填原始 LaTeX 源码（如 `\frac{a}{b}`），由前端 KaTeX 自动渲染。
- 变量表用清晰的符号列和解释列，移动端可改成单列。
- 公式框宽度跟正文一致，不能挤进右侧批注区，也不能遮挡阅读地图。
- 视觉上要和普通提示框区分：KaTeX 渲染的公式本体突出，变量解释可扫描，直觉读法有轻量背景。
- 行内短公式用 `<span class="formula" data-katex="...">文字回退</span>`，不展开为完整 formula-card。
- KaTeX 渲染失败时，如果 `formula-card__fallback-svg` 已预填充，则显示该 SVG；否则退到 `equation-plain` 纯文字。脚本不在浏览器端自动生成 SVG。

## SVG

- 每个重要图使用 `svg-figure`。
- 稍微复杂的机制、流程、对照、公式关系、调度逻辑、瓶颈链和概念依赖，必须用 SVG 图解，不只写长文字。
- 概念级图使用 `svg-figure concept-visual`，尺寸比主图更小，服务于当前概念，不展开成大图谱。
- 图后写"这张图怎么读"。
- SVG 只画动作链、因果链、变量关系或取舍关系；不要用装饰性图形填充页面。
- 如果一个解释需要读者在脑中同时保留三个以上对象，拆段并配图。
- 重组正文时，有用图保留为普通正文配图。

## Mermaid UML 图

- 类继承、接口实现、调用时序使用 `mermaid-figure uml-class` / `mermaid-figure uml-sequence`，详见 [mermaid.md](mermaid.md)。
- Mermaid 图的容器宽度跟正文一致，和 `svg-figure` 一样不进右侧批注区。
- 图后必须跟 `read-guide`，解释 UML 符号含义（继承箭头、菱形、生命线）。
- `<pre class="mermaid">` 内的 `<` 必须写成 `&lt;`，否则浏览器当成 HTML 标签吃掉。
- Mermaid 图和 SVG 图可以混用：公式关系用手写 SVG，UML 关系用 Mermaid，不要交叉。

## 验证

- 修改 HTML 后，使用当前 Agent 环境可用的浏览器自动化或本地预览服务器检查真实渲染。
- 验证至少覆盖：新文案存在、批注数量对齐、无重叠、主要断点可读。
